# SuperSkill router cases

Checked-in router fixtures must be synthetic and must not contain production task text,
paths, prompts, tokens, or customer data. Stage A requires at least 30 cases once exact
releases have real approval evidence.
