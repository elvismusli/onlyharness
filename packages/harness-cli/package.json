{
  "name": "onlyharness",
  "version": "0.2.17",
  "description": "SuperSkill universal installer and compatibility CLI (hh) for reviewed AI-agent capabilities",
  "type": "module",
  "license": "MIT",
  "homepage": "https://superskill.sh",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/elvismusli/onlyharness.git",
    "directory": "packages/harness-cli"
  },
  "keywords": [
    "ai-agents",
    "harness",
    "mcp",
    "superskill",
    "cli",
    "agent-workflows"
  ],
  "bin": {
    "hh": "./dist/hh.mjs",
    "onlyharness": "./dist/hh.mjs"
  },
  "files": [
    "dist/hh.mjs",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "smol-toml": "^1.7.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && npm run bundle",
    "bundle": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node20 --outfile=dist/hh.mjs && node -e \"const fs=require('node:fs');const p='dist/hh.mjs';let s=fs.readFileSync(p,'utf8');s=s.replace('#!/usr/bin/env node\\n','#!/usr/bin/env node\\nimport { createRequire } from ' + JSON.stringify('node:module') + ';const require = createRequire(import.meta.url);\\n');fs.writeFileSync(p,s);\"",
    "test": "npm run bundle && node --import tsx --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@harnesshub/capability-schema": "0.1.0",
    "@harnesshub/schema": "0.1.0",
    "@harnesshub/semantic-diff": "0.1.0",
    "@types/node": "^24.0.10",
    "@x402/evm": "^2.17.0",
    "@x402/fetch": "^2.17.0",
    "commander": "^14.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.3",
    "viem": "^2.54.6",
    "yaml": "^2.8.0"
  }
}
